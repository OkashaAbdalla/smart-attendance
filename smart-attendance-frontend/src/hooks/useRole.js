/**
 * useRole Hook
 * Purpose: Custom hook for role-based logic
 * Returns: { isStudent, isLecturer, role }
 * Usage: Conditional rendering based on user role
 * Note: Role comes from backend after authentication
 */

import useAuth from './useAuth';

const useRole = () => {
  // TODO: Extract and return role information
  return null;
};

export default useRole;
