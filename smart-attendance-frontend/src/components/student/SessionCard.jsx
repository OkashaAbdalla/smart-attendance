/**
 * Session Card Component (Student View)
 * Purpose: Display active attendance session details
 * Shows: Course name, time window, lecturer, mark attendance button
 * State: Active, expired, already marked
 */

const SessionCard = () => {
  // TODO: Implement session card with course info and CTA
  return null;
};

export default SessionCard;
